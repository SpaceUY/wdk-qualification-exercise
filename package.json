{
  "name": "wdk-qualification-exercise",
  "private": true,
  "scripts": {
    "dev": "turbo run start",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "build:worklet": "turbo run build:worklet"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
